version: "3.7"
services:
  httpd-server:
    build:
      context: .
    container_name: httpd-server
    hostname: httpd-server
    restart: on-failure
    ports:
      - "80:80"
    networks:
      - apache-net
    volumes:
      - ./:/www/
networks:
  apache-net:
    driver: bridge